apiVersion: apps/v1
kind: Deployment
metadata:
  name: 'library'
  namespace: dyegomaas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: library
  template:
    metadata:
      labels:
        app: library
    spec:
      containers:
      - name: library
        image: dyegomaas/library:latest
        ports:
        - containerPort: 80
        env:
          - name: "LIBRARY_SECRET"
            valueFrom:
              secretKeyRef:
                key:  library-secret
                name: library-auth     